name: Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "Running build.sh"
        run: ./.github/scripts/build.sh
        shell: bash
        env:
          NAME: "Mr. Prem"
